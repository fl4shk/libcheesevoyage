#!/usr/bin/env python3

from libcheesevoyage.general import *

class BusFromLayt(dict):
	def __init__(
		self,
		WIDTH_FROM: int,
class BusIshape(IntfShape):
	def __init__(
		self,
		WIDTH_FROM: int,
		WIDTH_TO: int,
		*,
		is_host: bool,
	):
		

a = Splitintf(BusIshape(WIDTH=4))
